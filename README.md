# employee-change-form v6

원인 수정:
- 템플릿 fetch는 성공했지만, 오른쪽 미리보기의 `#preview`가 flex 환경에서 폭 0으로 축소되어 양식이 보이지 않던 문제 수정
- `#preview`를 항상 미리보기 영역 전체 폭/높이로 확장
- 원본 양식 PNG 주소를 절대 URL로 보정
- 배경 이미지 로딩 실패 시 상태문구 표시
- 화면 버전표시 `v6`

기존 v5의 20개 양식, 흰색 마스크, 전자서명, A4 전체 미리보기는 유지합니다.
